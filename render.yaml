services:
  - type: web
    name: mychat-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node daily-whatsapp.js
    envVars:
      - key: NODE_VERSION
        value: 18
